-- Delta Compatible UI Engine
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- UI Container Safely Mounted for Delta
local parentTarget
if gethui then
    parentTarget = gethui()
elseif syn and syn.protect_gui then
    local gui = Instance.new("ScreenGui")
    syn.protect_gui(gui)
    gui.Parent = game:GetService("CoreGui")
    parentTarget = gui
elseif game:GetService("CoreGui"):FindFirstChild("RobloxGui") then
    parentTarget = game:GetService("CoreGui")
else
    parentTarget = LocalPlayer:WaitForChild("PlayerGui")
end

-- Cleanup duplicate
if parentTarget:FindFirstChild("StandardAppUI") then
    parentTarget.StandardAppUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "StandardAppUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = parentTarget

-- 2. Main Window Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 240, 0, 320)
MainFrame.Position = UDim2.new(0.5, -120, 0.4, -160)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 22, 28)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Parent = ScreenGui

local FrameCorner = Instance.new("UICorner")
FrameCorner.CornerRadius = UDim.new(0, 8)
FrameCorner.Parent = MainFrame

local FrameStroke = Instance.new("UIStroke")
FrameStroke.Color = Color3.fromRGB(45, 48, 58)
FrameStroke.Thickness = 1.2
FrameStroke.Parent = MainFrame

-- 3. Header Panel
local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Size = UDim2.new(1, 0, 0, 34)
Header.BackgroundColor3 = Color3.fromRGB(28, 30, 38)
Header.BorderSizePixel = 0
Header.Parent = MainFrame

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 8)
HeaderCorner.Parent = Header

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -20, 1, 0)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "BUILD A HOUSE PANEL"
TitleLabel.TextColor3 = Color3.fromRGB(0, 170, 255)
TitleLabel.TextSize = 12
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = Header

-- Dragging Functionality for Touch & Mouse
local dragging, dragInput, dragStart, startPos
Header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Header.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- 4. Scroll Container for Features
local Container = Instance.new("ScrollingFrame")
Container.Name = "Container"
Container.Size = UDim2.new(1, -16, 1, -44)
Container.Position = UDim2.new(0, 8, 0, 38)
Container.BackgroundTransparency = 1
Container.BorderSizePixel = 0
Container.ScrollBarThickness = 2
Container.CanvasSize = UDim2.new(0, 0, 0, 330)
Container.Parent = MainFrame

local UIList = Instance.new("UIListLayout")
UIList.Padding = UDim.new(0, 6)
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Parent = Container

-- Button Builder
local function AddToggle(text, order, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 32)
    btn.BackgroundColor3 = Color3.fromRGB(28, 30, 38)
    btn.Text = text .. " : OFF"
    btn.TextColor3 = Color3.fromRGB(180, 180, 190)
    btn.Font = Enum.Font.GothamMedium
    btn.TextSize = 11
    btn.LayoutOrder = order
    btn.Parent = Container

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(45, 48, 58)
    stroke.Thickness = 1
    stroke.Parent = btn

    local state = false
    btn.MouseButton1Click:Connect(function()
        state = not state
        if state then
            btn.BackgroundColor3 = Color3.fromRGB(16, 45, 30)
            btn.TextColor3 = Color3.fromRGB(0, 255, 150)
            btn.Text = text .. " : ON"
            stroke.Color = Color3.fromRGB(0, 255, 150)
        else
            btn.BackgroundColor3 = Color3.fromRGB(28, 30, 38)
            btn.TextColor3 = Color3.fromRGB(180, 180, 190)
            btn.Text = text .. " : OFF"
            stroke.Color = Color3.fromRGB(45, 48, 58)
        end
        callback(state)
    end)
end

-- ================= FEATURES INTEGRATION ================= --

-- 1. Full Auto Build
local autoBuildActive = false
AddToggle("Full Auto Build House", 1, function(state)
    autoBuildActive = state
    if state then
        task.spawn(function()
            while autoBuildActive do
                pcall(function()
                    for _, v in ipairs(workspace:GetDescendants()) do
                        if not autoBuildActive then break end
                        if v:IsA("ProximityPrompt") and (v.ActionText:lower():find("build") or v.ObjectText:lower():find("build") or v.Name:lower():find("build")) then
                            fireproximityprompt(v)
                            task.wait(0.1)
                        end
                    end
                end)
                task.wait(0.15)
            end
        end)
    end
end)

-- 2. Auto Get Materials
local autoMatActive = false
AddToggle("Auto Get Materials", 2, function(state)
    autoMatActive = state
    if state then
        task.spawn(function()
            while autoMatActive do
                pcall(function()
                    for _, v in ipairs(workspace:GetDescendants()) do
                        if not autoMatActive then break end
                        if v:IsA("ProximityPrompt") and (v.ActionText:lower():find("take") or v.ActionText:lower():find("get") or v.Name:lower():find("pickup")) then
                            fireproximityprompt(v)
                            task.wait(0.11)
                        end
                    end
                end)
                task.wait(0.2)
            end
        end)
    end
end)

-- 3. Auto Use Abilities
local autoAbilityActive = false
AddToggle("Auto Use Abilities", 3, function(state)
    autoAbilityActive = state
    if state then
        task.spawn(function()
            while autoAbilityActive do
                pcall(function()
                    local remotes = ReplicatedStorage:FindFirstChild("Remotes") or ReplicatedStorage:FindFirstChild("Events")
                    if remotes then
                        for _, r in ipairs(remotes:GetDescendants()) do
                            if r:IsA("RemoteEvent") and r.Name:lower():find("ability") then
                                r:FireServer()
                            end
                        end
                    end
                end)
                task.wait(1.5)
            end
        end)
    end
end)

-- 4. Auto Upgrade All
local autoUpActive = false
AddToggle("Auto Upgrade Powerups", 4, function(state)
    autoUpActive = state
    if state then
        task.spawn(function()
            while autoUpActive do
                pcall(function()
                    local remotes = ReplicatedStorage:FindFirstChild("Remotes") or ReplicatedStorage:FindFirstChild("Events")
                    if remotes then
                        for _, r in ipairs(remotes:GetDescendants()) do
                            if r:IsA("RemoteEvent") and r.Name:lower():find("upgrade") then
                                r:FireServer("CarryLimit")
                                r:FireServer("MovementSpeed")
                            end
                        end
                    end
                end)
                task.wait(0.8)
            end
        end)
    end
end)

-- 5. Movement Boost
local speedActive = false
AddToggle("Boost WalkSpeed (45)", 5, function(state)
    speedActive = state
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = state and 45 or 16
    end
end)
